function where(arr, num) {
    var index;
    // sort arr
    // iterate over arr
    // compare num to item at index
    // - if num is larger then continue
    // - if num is smaller, return index - 1
    return index;
}

where([40, 60], 50);
